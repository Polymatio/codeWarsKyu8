// Create a function that converts a number into a string.
// A toSting method must be used for conversion.
// want the string to be returned.

function numberToString(num) {
    return num.toString();
}
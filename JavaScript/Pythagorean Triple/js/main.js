// Create a function that determines if the non-negative integers form a pythagorean triple.

function isPythagoreanTriple(integers) {
    for(let i = 1; i < integers - 1; i++) {
        for(let j = i; b < integers; j++) {
            k = Math.sqrt(i * i + j * j);
            if(c % 1 === 0) {
                return integers
            }
        }
    }
}